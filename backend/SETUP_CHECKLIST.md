# ✅ Checklist: Setup โปรเจคหลัง Clone

## สำหรับคนที่ Clone โปรเจคครั้งแรก

### ก่อนเริ่ม
- [ ] ติดตั้ง SQL Server แล้ว
- [ ] ติดตั้ง SQL Server Management Studio (SSMS) แล้ว
- [ ] ติดตั้ง Java 17+ แล้ว
- [ ] ติดตั้ง Maven แล้ว

### ขั้นตอน Setup

#### 1. Database
- [ ] สร้าง Database: `CREATE DATABASE EventDB;`
- [ ] รันไฟล์: `sql/setup_all_tables.sql`
- [ ] (Optional) รันไฟล์: `data/sample-events-thai.sql`
- [ ] ตรวจสอบ: `sql/check_thai_support.sql` (ต้องเห็น ✅ ทั้งหมด)

#### 2. Backend Configuration
- [ ] แก้ไข `application.properties`:
  - `spring.datasource.password=YourPassword`
  - `jwt.secret=your-secret`
  - `TU_API_KEY=your-key` (ถ้ามี)

#### 3. รัน Backend
- [ ] รันคำสั่ง: `mvn spring-boot:run`
- [ ] เช็ค Console ไม่มี Error
- [ ] เปิด: http://localhost:8080/swagger-ui.html

#### 4. ทดสอบ
- [ ] ลองสร้าง Event ภาษาไทย
- [ ] ตรวจสอบว่าภาษาไทยแสดงผลถูกต้อง

---

## ⚠️ ถ้าเจอปัญหา

| ปัญหา | วิธีแก้ |
|-------|---------|
| ภาษาไทยเป็น ??? | รัน `fix_events_thai_support.sql` |
| Connection refused | ตรวจสอบ SQL Server เปิดอยู่หรือไม่ |
| Port 8080 in use | เปลี่ยน port ใน `application.properties` |

อ่านเพิ่มเติม: **FIX_THAI_LANGUAGE.md**

---

## 📞 ติดต่อ

ถ้ายังแก้ไม่ได้ ติดต่อคนในทีม!
