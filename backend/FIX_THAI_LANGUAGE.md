# 🔧 แก้ไขปัญหาภาษาไทยไม่ขึ้น

## ปัญหา: พิมพ์ภาษาไทยแล้วเป็น ??? หรือ □□□

### สาเหตุ
ตาราง Events เดิมเป็น **VARCHAR** (ไม่รองรับภาษาไทย)

---

## วิธีแก้ไข (เลือก 1 วิธี)

### ✅ วิธีที่ 1: แก้ไขตารางเดิม (ไม่ลบข้อมูล)

1. เปิด **SQL Server Management Studio**
2. เชื่อมต่อ Database: `EventDB`
3. รันไฟล์นี้:
   ```
   src/main/resources/sql/fix_events_thai_support.sql
   ```
4. Restart Spring Boot
5. ✅ เสร็จแล้ว!

---

### 🔄 วิธีที่ 2: ลบแล้วสร้างใหม่ (ข้อมูลจะหาย)

1. เปิด **SQL Server Management Studio**
2. รันไฟล์ตามลำดับ:
   ```
   1. drop_events_table.sql
   2. setup_all_tables.sql
   3. sample-events-thai.sql (ถ้าต้องการข้อมูลตัวอย่าง)
   ```
3. Restart Spring Boot
4. ✅ เสร็จแล้ว!

---

## ตรวจสอบว่าแก้ไขสำเร็จหรือไม่

รันไฟล์นี้:
```
src/main/resources/sql/check_thai_support.sql
```

ดูผลลัพธ์:
- ✅ = รองรับภาษาไทย (NVARCHAR)
- ❌ = ไม่รองรับภาษาไทย (VARCHAR)

---

## ยังไม่ได้?

ตรวจสอบ `application.properties`:

```properties
# ต้องมี charset=UTF-8
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=EventDB;encrypt=false;trustServerCertificate=true
```

แล้ว Restart Spring Boot อีกครั้ง
