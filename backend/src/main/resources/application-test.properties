###############################################
# TEST PROFILE - สำหรับทดสอบโดยไม่ใช้ TU API จริง
###############################################

###############################################
# DATABASE CONFIGURATION (Microsoft SQL Server)
###############################################
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=EventDB;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=YourStrong!Passw0rd
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

###############################################
# JPA / HIBERNATE SETTINGS
###############################################
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.default_schema=dbo

###############################################
# LOGGING
###############################################
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

###############################################
# SERVER CONFIGURATION
###############################################
server.port=8080

###############################################
# MOCK TU API CONFIG - ไม่ต้องใช้ API Key จริง
###############################################
TU_API_URL=http://localhost:8080/mock
TU_API_KEY=mock-test-key-not-used

###############################################
# SPRING APPLICATION INFO
###############################################
spring.application.name=project-CS261-TEST
spring.main.allow-bean-definition-overriding=true
spring.jackson.serialization.indent_output=true

###############################################
# 🧪 TEST MODE INDICATOR
###############################################
# Profile นี้จะใช้ MockTuAuthService แทน TuAuthService จริง
# ไม่ต้องมี TU API Key ที่ใช้งานได้จริง
