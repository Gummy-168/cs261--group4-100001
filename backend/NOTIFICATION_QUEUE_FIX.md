# 🔧 แก้ไขปัญหา Notification Queue Error

## 🔴 ปัญหาที่พบ
```
Error 400: could not execute statement [Invalid column name 'eventId'] 
[insert into dbo.notification_queue (eventId,sendat,status,userid) values (?,?,?,?)]
```

## ✅ สาเหตุ
ตาราง `notification_queue` **ยังไม่ได้ถูกสร้างในฐานข้อมูล** แต่โค้ด Java พยายามเขียนข้อมูลลงไป

---

## 🛠️ วิธีแก้ไข

### ขั้นตอนที่ 1: สร้างตาราง notification_queue

เปิด **SQL Server Management Studio** และรันคำสั่งนี้:

```sql
-- ตัวเลือก 1: สร้างเฉพาะตาราง notification_queue
```

รันไฟล์:
```
backend/src/main/resources/sql/create_notification_queue_table.sql
```

**หรือ**

```sql
-- ตัวเลือก 2: สร้างตารางทั้งหมดใหม่ (จะลบข้อมูลเดิม)
```

รันไฟล์:
```
backend/src/main/resources/sql/setup_all_tables.sql
```

---

### ขั้นตอนที่ 2: ตรวจสอบว่าตารางถูกสร้างแล้ว

```sql
USE EventDB;
GO

-- ตรวจสอบว่ามีตารางหรือยัง
SELECT name FROM sys.tables WHERE name = 'notification_queue';

-- ดูโครงสร้างตาราง
SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    IS_NULLABLE
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'notification_queue'
ORDER BY ORDINAL_POSITION;
```

ต้องเห็น columns ดังนี้:
- `id` - BIGINT (PRIMARY KEY)
- `user_id` - BIGINT (NOT NULL)
- `event_id` - BIGINT (NOT NULL)
- `send_at` - DATETIME2 (NOT NULL)
- `status` - NVARCHAR(20) (NOT NULL)
- `created_at` - DATETIME2
- `updated_at` - DATETIME2

---

### ขั้นตอนที่ 3: Restart Backend

1. หยุด Spring Boot backend
2. เริ่ม backend ใหม่:
   ```bash
   cd backend
   mvn spring-boot:run
   ```

---

### ขั้นตอนที่ 4: ทดสอบ

1. เข้าสู่ระบบที่ Frontend
2. ไปที่หน้ากิจกรรม
3. กดปุ่ม **บันทึก** (หัวใจ) ที่กิจกรรมใดกิจกรรมหนึ่ง
4. ตรวจสอบว่าไม่มี error 400 แล้ว

---

## 🔍 ตรวจสอบข้อมูล

```sql
-- ดูข้อมูลใน notification_queue
SELECT * FROM dbo.notification_queue;

-- นับจำนวน notification ของแต่ละ user
SELECT 
    u.username,
    u.displayname_th,
    COUNT(*) as notification_count
FROM dbo.notification_queue nq
JOIN dbo.users u ON nq.user_id = u.id
GROUP BY u.username, u.displayname_th;
```

---

## 📋 โครงสร้างตาราง notification_queue

| Column | Type | Description |
|--------|------|-------------|
| id | BIGINT | รหัสอัตโนมัติ |
| user_id | BIGINT | รหัสผู้ใช้ |
| event_id | BIGINT | รหัสกิจกรรม |
| send_at | DATETIME2 | เวลาที่จะส่งแจ้งเตือน (1 วันก่อนกิจกรรม) |
| status | NVARCHAR(20) | สถานะ: PENDING, SENT, FAILED |
| created_at | DATETIME2 | วันที่สร้าง |
| updated_at | DATETIME2 | วันที่อัปเดต |

---

## 🎯 ฟังก์ชันที่เกี่ยวข้อง

### 1. บันทึกกิจกรรมที่สนใจ
```java
POST /api/favorites
{
  "userId": 1,
  "eventId": 5
}
```
- เพิ่มลงในตาราง `favorites`
- สร้าง notification ใน `notification_queue` (ส่ง 1 วันก่อนกิจกรรม)

### 2. ยกเลิกการบันทึก
```java
DELETE /api/favorites/{userId}/{eventId}
```
- ลบออกจาก `favorites`
- ลบ notification ที่เกี่ยวข้อง

---

## ⚠️ หมายเหตุ

1. **Foreign Keys:** ตารางนี้เชื่อมกับ `users` และ `events` ดังนั้น:
   - ต้องมีตาราง `users` และ `events` อยู่ก่อน
   - ถ้าลบ user หรือ event, notification ที่เกี่ยวข้องจะถูกลบด้วย (CASCADE)

2. **Status Values:**
   - `PENDING` = รอส่ง
   - `SENT` = ส่งแล้ว
   - `FAILED` = ส่งไม่สำเร็จ

3. **send_at:** คำนวณอัตโนมัติเป็น `event.startTime - 1 day`

---

## 🐛 ถ้ายังมีปัญหา

### Error: Foreign Key constraint failed
```
สาเหตุ: ไม่มีตาราง events หรือ users
แก้ไข: สร้างตารางเหล่านี้ก่อน
```

### Error: Cannot insert NULL
```
สาเหตุ: ข้อมูล user_id, event_id, send_at เป็น NULL
แก้ไข: ตรวจสอบโค้ด Java ว่าส่งค่ามาครบหรือไม่
```

---

## 📞 ติดต่อ

หากยังพบปัญหา:
1. ตรวจสอบ Console log ใน Browser (F12)
2. ตรวจสอบ Backend log
3. ส่ง error message มาให้ช่วยวิเคราะห์
